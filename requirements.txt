## ==================================================================
# SPARKII RAG: Python Dependencies (Railway Optimized)
# ==================================================================
# CPU-only PyTorch for 75% size reduction (888MB → 200MB)
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# Core ML dependencies
torch>=2.5.0
sentence-transformers==2.7.0
numpy>=1.24.0

# Database - Python 3.13 compatible
psycopg[binary]>=3.1.0  # psycopg3 is Python 3.13 compatible
pgvector==0.2.5

# FastAPI - Python 3.13 compatible
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# OpenRouter client - Python 3.13 compatible
openai>=1.54.0

# Utilities
python-dotenv==1.0.1
tqdm==4.66.4
